# Terminal Setup

Documents the terminal environment: Kitty, tmux, readline, fish shell config.

**Last updated:** 2026-03-05

---

## Kitty (`~/.config/kitty/kitty.conf`)

**Theme:** Neon Orchid Theme (custom, generated by Claude). Deep purple background (`#1a0a1f`), pink accents (`#ff69eb`), neon highlights.

**Font:** DaddyTimeMono Nerd Font Mono, 12pt

**Key settings:**
```
macos_option_as_alt yes   # Option key sends Alt/Meta escape sequences
                          # (enables readline Alt+b/f/d, tmux Alt+hjkl)
                          # tradeoff: macOS Option special chars (é, ü, ˙) unavailable in Kitty
background_opacity 0.90
background_blur 1
window_padding_width 4
```

---

## tmux (`~/.config/tmux/tmux.conf`)

**Prefix:** `Ctrl+Space`
- Changed from default `Ctrl+b` because Claude Code binds `Ctrl+b` to `task:background`

**Pane navigation:** `Alt+h/j/k/l` (root-level, no prefix)
- Root-level bindings bypass the prefix mechanism entirely
- Avoids a bug where `prefix + o` swaps pane content (instead of cycling focus) when
  Claude Code's approval UI is active — caused by mouse-mode terminal flag changes
- `prefix + o` still exists but is unreliable; use Alt+hjkl instead

**Other bindings:**
- `prefix + h` — popup cheatsheet (`~/.config/tmux/cheat.md`, rendered with `glow`)
- `prefix + [` — copy mode (vi keys: `v` select, `y` copy to clipboard via `pbcopy`)
- Mouse on: resize panes by dragging borders, click to select

**Status bar:** Claude usage tracker (right side, 60s refresh)
```
~/claude-projects/claude-usage/claude_usage.py tmux
```

**Cheatsheet** (`~/.config/tmux/cheat.md`): covers tmux, readline, and Amethyst shortcuts.

---

## Readline

Built into bash/fish/most REPLs — no config file needed. Cheatsheet in tmux popup.

Key bindings (emacs mode, active everywhere including Claude Code input):

| Key | Action |
|-----|--------|
| `Ctrl+a` / `Ctrl+e` | beginning / end of line |
| `Ctrl+b` / `Ctrl+f` | back / forward one char |
| `Alt+b` / `Alt+f` | back / forward one word |
| `Ctrl+k` / `Ctrl+u` | kill to end / start of line |
| `Ctrl+w` / `Alt+d` | kill word backward / forward |
| `Ctrl+y` | yank (paste killed text) |
| `Ctrl+r` | reverse history search |
| `Ctrl+l` | clear screen |

> `Alt+b/f/d` require `macos_option_as_alt yes` in Kitty to work.

---

## Claude Code keybindings (`~/.claude/keybindings.json`)

Currently empty (all defaults preserved). Default bindings use:
- `Ctrl+b` → `task:background` (Task context only — no readline conflict since Task context has no text input)
- `Ctrl+o` → `app:toggleTranscript` (Global)
- `Ctrl+t` → `app:toggleTodos` (Global)
